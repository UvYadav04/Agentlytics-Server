-r ../shared/requirements.txt
fastapi
uvicorn[standard]

# Auto-instruments FastAPI with OTel spans + request count/latency/error/active-request metrics,
# exported via the OTLP pipeline set up in shared/observability.py - see main.py.
opentelemetry-instrumentation-fastapi
