name: Deploy Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_TOKEN }}" | docker login \
            --username "${{ secrets.DOCKER_USERNAME }}" \
            --password-stdin

      - name: Build WORKER
        run: |
          docker build \
            -t itzdineshyadav/data-analyzer:worker \
            -f worker_service/Dockerfile \
            .

      - name: Build API
        run: |
          docker build \
            -t itzdineshyadav/data-analyzer:api \
            -f api_service/Dockerfile \
            .



      - name: Push API
        run: docker push itzdineshyadav/data-analyzer:api

      - name: Push Worker
        run: docker push itzdineshyadav/data-analyzer:worker

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /opt/data-analyzer/scripts
            ./deploy-server.sh
